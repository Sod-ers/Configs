# The essentials:

# Downloads directory
alias dl="cd ~/Downloads"
alias DL="cd ~/Downloads"

# Create new crontab
alias cb="crontab -e"
alias CB="crontab -e"

# Refresh aliases
alias rf=". ~/.bashrc"
alias RF=". ~/.bashrc"

# Create new aliases
alias ali="nano ~/.bash_aliases"
alias ALI="nano ~/.bash_aliases"

# Clear terminal
alias c="clear"
alias C="clear"
